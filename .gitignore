# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Env / secrets
.env
.env.local

# Local offline store (mimics Storage buckets in OFFLINE_MODE)
.localdata/

# Build / OS
*.log
.DS_Store
dist/
build/

# Test artifacts
.pytest_cache/
.coverage
htmlcov/

# Real test fixtures (real business data — keep out of git; see fixtures/README)
tests/fixtures/Expiry_Log.xlsx
tests/fixtures/ticket_*.jpg

# PHI: real patient ticket photos must NEVER be committed (BUILD_SPEC §9). Drop
# them in locally to verify redaction; these patterns keep them out of git.
tests/fixtures/real/
*.jpeg
MH*.jpg
MO*.jpg
